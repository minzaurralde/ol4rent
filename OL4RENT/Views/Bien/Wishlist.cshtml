@model IEnumerable<Ol4RentAPI.Model.Bien>
@{
    ViewBag.Title = "Wishlist";
}

<h2>Mi Wishlist</h2>
<ul>
@foreach (var item in Model) {
    <li>
        <div>
            @Html.ActionLink(item.Titulo, "Details", "Bien", new { id=item.Id }, null)
        </div>
        <div>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </div>
    </li>
}
</ul>